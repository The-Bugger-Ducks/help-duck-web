export enum SortTableTypes {
  priority = "priorityLevel",
  title = "title",
  createdAt = "createdAt",
  status = "status",
}

export enum OrderByTypes {
  asc = "asc",
  desc = "desc",
  none = ""
}